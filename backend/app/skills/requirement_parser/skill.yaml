name: requirement_parser
version: "1.0.0"
title: 需求解析
description: 从 PRD 文本提取结构化功能点
category: core
stage: requirement
tags: [requirement, core]

ui:
  selectable: false

inputs:
  raw_content:
    type: string
    required: true

outputs:
  items:
    type: array
